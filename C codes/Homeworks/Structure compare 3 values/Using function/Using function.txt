#include <stdio.h>
int comparison(int price1, int price2, int price3)
{
    int result;
    if(price1>price2 && price1>price3)
    {
        result=price1;
        /*printf("the highest priced book is Aadujeevitham");*/
        return result;
    }
    
    if(price2>price1 && price2>price3)
    {
        result=price2;
        /*printf("The highest priced boook is Wings of fire");*/
        return result;
    }
    
    if(price3>price1 && price3>price2)
    {
        result=price3;
        /*printf("The highest priced book is Balyakalasakhi");*/
        return result;
    }
}

typedef struct{
    char book_name[50];
    char author_name[50];
    int price;
}book;

int main()
{
    int highest;
    
    book book1 = {"Aadujeevitham","Benyamin",250};
    book book2 = {"Wings of fire","A P J Abdul kalam",200};
    book book3 = {"Balyakalasakhi","Vaikom muhammad basheer",150};
    
    printf(" Book name: %s, Author: %s, Price: %d\n",book1.book_name,book1.author_name,book1.price);
    printf(" Book name: %s, Author: %s, Price: %d\n",book2.book_name,book2.author_name,book2.price);
    printf(" Book name: %s, Author: %s, pricee: %d\n",book3.book_name,book3.author_name,book3.price);
    
    highest = comparison(book1.price,book2.price,book3.price);
    if (highest == book1.price)
    {
        printf(" The highest priced book is 'Aadujeevitham'");
    }
    else if(highest == book2.price)
    {
        printf(" The highest priced book is 'Wings of fire'");
    }
    else if(highest == book3.price)
    {
        printf(" The highest priced book is 'Balyakalasakhi'");
    }
    else
    {
        printf(" Oooops Something unusual happened check the code again");
    }
   
}