BASIC REFRESHER

1. Write a C program that takes an integer from the user and determines whether
the number is even or odd.

Ans:

#include <stdio.h>

int main()
{
    int a;
    
    printf("Enter an integer  to check if it is even or odd ");
    scanf("%d",&a);
    
    if(a == 0)
    {
        printf("The given number is 0.\n");
    }
    else if(a%2 == 0)
    {
        printf("The given number is an even number.\n");
    }
    else
    {
        printf("The given number is an odd number.\n");
    }
    
   
    return 0;
}

2. Write a C program that asks the user to enter a number and determines whether that
number is prime or not.

Ans:

#include <stdio.h>

int main()
{
    int n, i, prime = 0;
    
    printf("Enter a number\n");
    scanf("%d", &n);
    
    for(i=2; i<n; i++)
    {
        if(n % i == 0)
        {
            prime = 1;
            break;
        }
    }
    
    if(n == 1)
    {
        printf("Enter a number greater than 1\n");
    }
    else if(prime == 0)
    {
        printf("The given number is prime\n");
    }
    else
    {
        printf("The given number is not prime\n");
    }

    return 0;
}

3. Write a C program that takes a positive integer from the user and calculates its
factorial.

Ans:

#include <stdio.h>

int main()
{
    int num, i, fact = 1;
    
    printf("Enter a number\n");
    scanf("%d",&num);
    
    for(i=1; i<=num; i++)
    {
        fact = fact * i;
    }
    
    printf("factorial of %d is %d",num, fact);
    
    return 0;
}

4. Write a C program to generate the Fibonacci series up to a given number of terms.

Ans:

#include <stdio.h>

int main()
{
    int n, i, f0 = 0, f1 = 1, fn;
    
    printf("Enter a number\n");
    scanf("%d", &n);
    
    for(i = 1; i <= n; i++)
    {
        printf("%d ", f0);
        fn = f0 + f1;
        f0 = f1;
        f1 = fn;
    }
    
    return 0;
}

Functions

5. Write a C program that uses a function to check whether a given number is a
Perfect Number.

Ans:

#include <stdio.h>

void perfect(int n)
{
    int i, sum = 0;
    for(i = 1; i < n; i++)
    {
        if(n%i == 0)
        {
            sum = sum+i;
        }
    }
    
    if(sum == n)
    {
        printf("%d is perfect number", n);
    }
    else
    {
        printf("%d is not perfect number", n);
    }
}

int main()
{
    int num;
    
    printf("Enter a number\n");
    scanf("%d", &num);
    
    perfect(num);
    
    return 0;
}

6. Write a C program that uses a recursive function to find the GCD of two
numbers using the Euclidean algorithm.

Ans:


#include <stdio.h>

int gcd(int,int);

int main()

{
   int num1, num2;
   
   printf("Enter 2 positive integer\n");
   scanf("%d %d", &num1,&num2);
   
   printf("GCD of %d and %d is %d",num1,num2,gcd(num1,num2));
}

int gcd(int n1, int n2)
{
    if(n1 == 0)
    {
        return(n2);
    }
    
    if(n2 == 0)
    {
        return(n1);
    }
    
    
    if(n1 > n2)
    {
        return(gcd(n1 % n2, n2));
    }
    
    else 
    {
        return(gcd(n1, n2 % n1));
    }  
}

7. Write a C program that uses a function to count the frequency of each digit (0–9)
in a given integer.

Ans:

#include <stdio.h>

void countDigits(int n, int freq[]);

int main()
{
    int n, i;
    int freq[10] = {0};

    printf("Enter an integer: ");
    scanf("%d", &n);

    if (n < 0)
    {
        n = -n;
    }

    countDigits(n, freq);

    printf("Digit frequency:\n");
    for(i = 0; i < 10; i++)
    {
        printf("%d occurs %d times\n", i, freq[i]);
    }

    return 0;
}

void countDigits(int n, int freq[])
{
    if (n == 0)
    {
        freq[0]++;
        return;
    }

    while(n > 0)
    {
        int digit = n % 10;
        freq[digit]++;
        n = n / 10;
    }
}

8. Write a C program that uses functions to convert a decimal number to any base
between 2 and 16.

Ans: 

#include <stdio.h>

int main()
{
    int num,base,count=0,temp,i;
    int conv[100];
    
    printf("Enter a decimal integer\n");
    scanf("%d",&num);
    
    printf("Enter a base to which you want to convert(2,8,16)\n");
    scanf("%d",&base);
    
    if(base<=16)
    {
        temp = num;
        while(temp>0)
        {
            conv[count++] = temp % base;
            temp /= base;
        }
        printf("\n %d in base %d : ",num,base);
        for(i=count-1;i>=0;i--)
        {
            if(conv[i]<10)
            {
                printf("%d",conv[i]);
            }
            else
            {
                printf("%c",'A'+conv[i]-10);
            }
        }
    }
    else
    {
        printf("Invalid base");
    }
    
   return 0;
}


9.Write a C program that uses functions to evaluate a simple arithmetic expression
given two numbers and an operator.

Ans:

#include <stdio.h>

float Add(float,float);
float Sub(float,float);
float Mul(float,float);
float Div(float,float);

int main()

{
   float num1, num2, Result;
   char choice;
   
   printf("Enter the 1st No: ");
   
   scanf("%f",&num1);
   
   printf("\nSelect the operator: '+','-','*','/' : ");
   
   scanf(" %c", &choice);
   
   printf("\nEnter the 2nd No: ");
   
   scanf("%f",&num2);
   
   switch(choice)
   {
       case '+': 
       Result = Add(num1,num2);
       printf("\nResult is = %.2f",Result);
       break;
       
       case '-': 
       Result = Sub(num1,num2);
       printf("\nResult is = %.2f",Result);
       break;
       
       case '*': 
       Result = Mul(num1,num2);
       printf("\nResult is = %.2f",Result);
       break;
       
       case '/': 
       if(num2 != 0)
       {
           Result = Div(num1,num2);
           printf("\nResult is = %.2f",Result);
       }
       else
       {
           printf("Number can't be divided by \"0\"");
       }
       
       break;
       
       default: printf("Wrong choice entered\n");
       break;
       
   }
 return 0;
}

float Add(float n1, float n2)
{
    return n1 + n2;
}

float Sub(float n1, float n2)
{
    return n1 - n2;
}

float Mul(float n1, float n2)
{
    return n1 * n2;
}

float Div(float n1, float n2)
{
    return  n1 / n2;
}

10. Write a C program that takes a string of digits from the user and converts it into its equivalent
integer value.

Ans:

#include <stdio.h>
#include <string.h>

int main()
{
  char str[25];
  int sum=0,i;
  
  printf("Enter a String: \n");
  gets(str);
  
  for(i=0;i<strlen(str);i++)
  {
      sum=(sum*10)+(str[i]-48);
  }
  printf("%d",sum);
  return 0;
}

11. Write a C program that takes an integer from the user and converts it into a
string of characters.

Ans:

#include <stdio.h>

int main()
{
    int n, temp, i = 0, j;
    char str[20];

    printf("Enter a whole number: ");
    scanf("%d", &n);

    temp = n;

    if (n == 0)
    {
        str[i++] = '0';
    }
    else
    {
        if (n < 0)
        {
            n = -n;
        }

        while (n > 0)
        {
            str[i++] = (n % 10) + '0';
            n = n / 10;
        }

        if (temp < 0)
        {
            str[i++] = '-';
        }

        str[i] = '\0';

        for (j = 0; j < i / 2; j++)
        {
            char ch = str[j];
            str[j] = str[i - 1 - j];
            str[i - 1 - j] = ch;
        }
    }

    if (temp == 0)
    {
        str[i] = '\0';
    }

    printf("String: %s\n", str);

    return 0;
}

12. Write a C program that takes a string from the user and checks whether it is a
palindrome.

Ans:

#include <stdio.h>
#include <string.h>

int main()

{
    char str[100];
    int i, len, flag=0;
    
    printf("Enter your string\n");
    scanf("%s",str);
    
    len = strlen(str);
    
    for(i = 0; i < len; i++)
    {
        if(str[i] != str[len-i-1])
        {
            flag = 1;
            break;
        }
    }
    
    if(flag == 1)
    {
        printf("\nstring is not palindrome");
    }
    else
    {
        printf("\nstring is palindrome");
    }
    
   return 0;
}

13. Write a C program that takes a string from the user and reverses its characters using
an iterative approach.

Ans:


#include <stdio.h>
#include <string.h>

int main()

{
    char str[25], rev[25];
    int i, j = 0, loop = 0;
    
    printf("Enter the string\n");
    scanf("%s",str);
    
    for(i=0; str[i] != '\0'; i++)
    {
        loop++;
    }
    
    for(i=loop-1; i>=0; i-- )
    {
        rev[j] = str[i];
        j++;
    }
    rev[j] = '\0';
    
    printf("Reversed string is %s",rev);
    
   return 0;
}

14. Write a C program that takes a string input from the user and determines whether it is
a pangram or not.

Ans:

#include<stdio.h>
#include<string.h>

int main()
{
    char s[100];
    int i,total=0,used[26]={0};
    
    printf("Enter the string\n");
    gets(s);
    
    for(i=0;s[i]!='\0';i++)
    {
        if('a'<= s[i] && 'z'>=s[i])
        {
            total+=!used[s[i]-'a'];
            used[s[i]-'a']=1;
        }
        else if('A'<=s[i]&&'Z'>=s[i])
        {
            total+=!used[s[i]-'A'];
            used[s[i]-'A']=1;
        }
    }
    if(total == 26)
    {
        printf("The given string is Pangram");
    }
    else
    {
        printf("The given string is not pangram");
    }
    return 0;
}

15. Write a C program that takes a string from the user and replaces every sequence of
multiple consecutive spaces with a single space.

Ans:

#include <stdio.h>

void main()
{
  char str[50];
  int i,j;
  
  printf("Enter a String: \n");
  gets(str);
  
  for(i=0;str[i];i++)
  {
      if(str[i]==32 && str[i+1]==32)
      {
          for(j=i;str[j];j++)
          {
              str[j]=str[j+1];
          }
           i--;
      }
  }
  printf("%s\n",str);
}


16. Write a C program that takes a string from the user and converts all uppercase letters
into their corresponding lowercase letters.

Ans:

#include <stdio.h>

void main()
{
  char str[50];
  int i;
  
  printf("Enter a string\n");
  gets(str);
  
  for(i=0;str[i]!='\0';i++)
  {
      if(str[i]>='A' && str[i]<='Z')
      {
        str[i]=str[i]+32;
      }
      else if(str[i]>='a' && str[i]<='z')
      {
          str[i]=str[i]-32;
      }
  }
  str[i]='\0';
  printf("Resulting string is %s\n",str);
}


17. Write a C program that takes a number and toggles a specific number of bits (n)
starting from a given position.

Ans:


#include <stdio.h>

int main()
{
   int n,bp;
   
   printf("Enter number\n");
   scanf("%d",&n);
   printf("Enter bit position\n");
   scanf("%d",&bp);
   
   printf("Before operation n = %d\n",n);
   
   n = n^(1<<bp);
   
   printf("after the operation n = %d\n",n);
   
   return 0;
  
}

18.Write a C program that takes an integer from the user and extracts (n) bits starting
from a specific bit position.

Ans:

#include<stdio.h>

int extract_bits(int num, int n, int pos)
{
    // Edge case,1: No bits to be extracted
    if(n==0)
    {
        printf("No bits to be extracted");
        return -1;
    }
    // Edge case,2: Invalid position
    if(pos<0 || pos>=(sizeof(int)*8))
    {
        printf("Invalid position");
        return -1;
    }
    // Edge case,3: Invalid number of bits
    if(n<0 || n> ((sizeof(int) * 8) -pos))
    {
        printf("Invalid number of bits");
        return -1;
    }
    // Create a mask of n 1's
    int mask= ((1<<n)-1)<<pos;
    // Extract n bits
    int bits= num & mask;
    // Shift back to LSB
    bits=bits>>pos;

    return bits;
}
int main()
{
    int num,n,position;
    printf("Enter your number: ");
    scanf("%d",&num);

    printf("\nEnter number of bits to be extracted: ");
    scanf("%d",&n);

    printf("\nEnter position: ");
    scanf("%d",&position);
    
    printf("\nResulting number: %d ",extract_bits(num,n,position));

    return 0;
}

19. Write a C program that takes a number and replaces n of its bits, starting from a
given position, with another set of n bits from a second number.

Ans:

#include<stdio.h>

void replace_bits(int *dest, int src, int n, int pos)
{
    // Edge case,1: No bits to be replaced
    if(n==0)
    {
        printf("No bits to be replaced");
        return ;
    }
    // Edge case,2: Invalid position
    if(pos<0 || pos>=(sizeof(int)*8))
    {
        printf("Invalid position");
        return ;
    }
    // Edge case,3: Invalid number of bits
    if(n<0 || n> ((sizeof(int) * 8) -pos))
    {
        printf("Invalid number of bits");
        return ;
    }
    // Create a mask of n 1's
    int mask= ((1<<n)-1)<<pos;
    // Copy n bits from source
    int bits= src & mask;
    // Clear n bits of destination
    *dest=*dest & ~mask;
    // Insert source bits into destination
    *dest =*dest| bits;
    
}
int main()
{
    int dest,src,n,position;

    printf("Enter your destination number: ");
    scanf("%d",&dest);

    printf("\nEnter your source number: ");
    scanf("%d",&src);

    printf("\nEnter number of bits to be extracted: ");
    scanf("%d",&n);

    printf("\nEnter position: ");
    scanf("%d",&position);
    
    replace_bits(&dest,src,n,position);

    printf("\nResulting number: %d", dest);

    return 0;
}

20.Write a C program that takes two integers and swaps n bits starting from a given
position in both numbers.

Ans:

#include <stdio.h>


void swap_bits(int *a, int *b, int n, int pos)
{
    // Edge case,1: No bits to swap
    if(n==0)
    {
        printf("No bits swapped");
        return;
    }

    // Edge case,2: Invalid position
    if(pos<0 || pos>=(sizeof(int)*8))
    {
        printf("Invalid position");
        return ;
    }

    // Edge case,3: Invalid number of bits
    if(n<0 || n> ((sizeof(int) * 8) -pos))
    {
        printf("Invalid number of bits");
        return ;
    }

    // Loop through n bits from given position
    for(int i=pos;i<(pos+n);i++)
    {
        int bit_a = (*a>>i)&1;
        int bit_b = (*b>>i)&1;
        // If bits aren't same, toggle both bits
        if(bit_a!=bit_b)
        {
            *a^=(1<<i);
            *b^=(1<<i);
        }
    }
   
}
int main()
{
    int a,b,n,position;
    printf("Enter first number: ");
    scanf("%d",&a);

    printf("\nEnter second number: ");
    scanf("%d",&b);

    printf("\nEnter number of bits to be extracted: ");
    scanf("%d",&n);

    printf("\nEnter position: ");
    scanf("%d",&position);
    
    swap_bits(&a,&b,n,position);

    printf("\nResult after swap: %d  %d", a, b);

    return 0;
}

21. Write a C program to remove duplicate elements from a given array.

Ans:


#include <stdio.h>

int main()

{
   int arr[30], size, i, j, k;
   
   printf("Enter the array size\n");
   scanf("%d",&size);
   
   printf("\nEnter the array elements\n");
   for(i=0;i<size;i++)
   {
       scanf("%d",&arr[i]);
   }
   
   printf("The array elements are\n");
   for(i=0;i<size;i++)
   {
       printf("%d",arr[i]);
   }
   
   printf("\nArray after remove duplicates\n");
   for(i=0;i<size;i++)
   {
       for(j=i+1;j<size;j++)
       {
           if(arr[i]==arr[j])
           {
               for(k=j;k<size-1;k++)
               {
                   arr[k]=arr[k+1];
               }
               size--;
               j--;
           }
       }
   }
   for(i=0;i<size;i++)
   {
       printf("%d",arr[i]);
   }
    
   return 0;
}

22.Write a C program that uses an array to find the second largest and second
smallest element in a list of numbers.

Ans:


#include <stdio.h>

#define MAX_SIZE 20
#define MIN_VALUE -30000
#define MAX_VALUE 30000

int main()
{
  int arr[MAX_SIZE], largest=MIN_VALUE, sec_largest=MIN_VALUE, i, n;
  int smallest=MAX_VALUE, sec_smallest=MAX_VALUE;
  printf("Enter the array size\n");
  scanf("%d",&n);
  
  printf("Enter the array elements\n");
  for(i=0;i<n;i++)
  {
      scanf("%d",&arr[i]);
  }
  
  for(i=0;i<n;i++)
  {
      if(arr[i]>largest)
      {
          sec_largest=largest;
          largest=arr[i];
      }
      else if(arr[i]!=largest && arr[i]>sec_largest)
      {
          sec_largest = arr[i];
      }
  }
  if(sec_largest != MIN_VALUE)
  {
      printf("\nSecond largest element = %d", sec_largest);
  }
  else
  {
      printf("\n No second largest");
  }
  
  for(i=0;i<n;i++)
  {
      if(arr[i]<smallest)
      {
          sec_smallest = smallest;
          smallest = arr[i];
      }
      else if(arr[i]!=smallest && arr[i]<sec_smallest)
      {
          sec_smallest = arr[i];
      }
  }
  
  if(sec_smallest != MAX_VALUE)
  {
      printf("\nSecond smallest = %d", sec_smallest);
  }
  else
  {
      printf("\nNo second smallest");
  }
   return 0;
}

23.Write a C program that rotates the elements of an array by n positions in a
specified direction.

Ans:

#include <stdio.h>

int main()
{
  int pos,dir,temp,temp2,i,n=25;
  int arr[n];
  
  printf("Enter array size\n");
  scanf("%d",&n);
  
  printf("Enter the array elements\n");
  for(i=0;i<n;i++)
  {
      scanf("%d",&arr[i]);
  }
  
  printf("Array elements are\n");
  for(i=0;i<n;i++)
  {
      printf("%d\t",arr[i]);
  }
  
  
  printf("\nEnter the direction to shift 1:LEFT 2:RIGHT\n");
  scanf("%d",&dir);
  
  printf("\nEnter no of positions to shift\n");
  scanf("%d",&pos);
  
  
  while(pos--)
  {
      if(dir == 1)
      {
          temp = arr[0];
          for(i=0;i<n;i++)
          {
              arr[i] = arr[i+1];
          }
          arr[n-1] = temp;
      }
      else if(dir == 2)
      {
          temp2 = arr[n-1];
          for(i=n-1;i>0;i--)
          {
              arr[i]=arr[i-1];
          }
          arr[0]=temp2;
      }
      else
      {
          printf("wrong direction\n");
      }
  }
  
  
  printf("Shifted array elements are\n");
  for(i=0;i<n;i++)
  {
      printf("%d\t",arr[i]);
  }
  
   return 0;
}

24.Write a C program that merges two sorted arrays into a single sorted array
without using any sorting function.

Ans:

#include <stdio.h>

int main()
{
    int arr1[10],s1,arr2[10],s2,res[20],s3;
    int i,j,k=0;
    
    printf("Enter 1st sorted array size within '10'\n");
    scanf("%d",&s1);
    
    printf("Enter array elements\n");
    for(i=0;i<s1;i++)
    {
        scanf("%d",&arr1[i]);
    }
    
    printf("Enter 2nd sorted array size within '10'\n");
    scanf("%d",&s2);
    
    printf("Enter array elements\n");
    for(j=0;j<s2;j++)
    {
        scanf("%d",&arr2[j]);
    }
    
    s3=s1+s2; 
    
    i=0;
    j=0;
    
    while(i<s1 && j<s2)
    {
        if(arr1[i]<arr2[j])
        {
            res[k]=arr1[i];
            i++;
            k++;
        }
        else
        {
            res[k]=arr2[j];
            j++;
            k++;
        }
    }
    while(i<s1)
    {
        res[k++]=arr1[i++];
    }
    while(j<s2)
    {
        res[k++]=arr2[j++];
    }
    
    printf("\nMerged array");
    
    for(k=0;k<s3;k++)
    {
        printf("%d\t",res[k]);
    }
    return 0;
}

25.Write a C program that finds all pairs of elements in an array whose sum equals
a given target value.

Ans:

#include<stdio.h>

int main()
{
    int i,j,k,n,add,flag=0;
    printf("Enter size of array: ");
    scanf("%d",&n);
    int a[n];
    
    printf("your sum to find pairs: ");
    scanf("%d",&add);
    
    printf("Enter elements of array: ");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    printf("The required numbers are: ");
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]+a[j] == add)
            {
                flag=1;
                printf("(%d,%d) found at [%d,%d]\n",a[i],a[j],i+1,j+1);
            }
        }
    }
    if(flag == 0)
    printf("NOT FOUND\n");
    return 0;
}

26. Write a C program that rearranges an array such that all negative numbers
appear before all positive numbers while preserving the relative order of both
groups.

Ans:
#include <stdio.h>

int main()
{
    int i,j,temp,n;
    int arr[10];
    
    printf("Enter array size within 10\n");
    scanf("%d",&n);
    
    if (n>10 || n<=0)
    {
        printf("Invalid size\n");
        return 0;
    }
    
    printf("Enter array elements\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    
    i=0;
    j=n-1;
    
    while(1)
    {
        while(arr[i]<0 && i<j)
        {
            i++;
        }
        while(arr[j]>0 && i<j)
        {
            j--;
        }
        if(i<j)
        {
           temp=arr[i];
           arr[i]=arr[j];
           arr[j]=temp;
        }
        else
        {
            break;
        }
        
    }
    printf("Array elements are:\n");
    
    for(i=0;i<n;i++)
    {
        printf("%d\t",arr[i]);
    }
    return 0;
}

27.Define a structure Complex with real and imaginary parts. Write a C program to add
two complex numbers and display the result.

Ans:

#include<stdio.h>

struct complex
{
  float real;
  float imag;
}a,b,c;

struct complex accept(void);
void display(struct complex);
struct complex add(struct complex, struct complex);

int main()
{
    printf("Enter the 1st complex number\n");
    a = accept();
    display(a);
    
    printf("\nEnter the 2nd complex number\n");
    b = accept();
    display(b);
    
    printf("\nAddition\n");
    c = add(a,b);
    display(c);
}

struct complex accept(void)
{
    struct complex t;
    printf("Enter the real part\n");
    scanf("%f",&t.real);
    printf("Enter the imaginary part\n");
    scanf("%f",&t.imag);
    return t;
}

void display(struct complex a)
{
    printf("complex number is\n");
    printf("%.f + %.fi", a.real, a.imag);
    printf("\n");
}

struct complex add(struct complex p,struct complex q)
{
    struct complex t;
    t.real = (p.real + q.real);
    t.imag = (p.imag + q.imag);
    return t;
}

