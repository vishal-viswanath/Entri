#include <stdio.h>
void add(int a, int b)
{
    int sum;
    sum = a+b;
    printf("sum of 2 number is: %d", sum);
}
void sub(int a, int b)
{
    int dif;
    dif=a-b;
    printf("\nSub of 2 number is: %d", dif);
}
void div(int a, int b)
{
    int divi;
    divi=a/b;
    printf("\ndivision of 2 number is: %d", divi);


}
void mul(int a, int b)
{
    int multi;
    multi=a*b;
    printf("\nMultiplication of 2 number is: %d", multi);
}
int main()
{
    int num1, num2;
    int result;
    printf("enter your 1st number: ");
    scanf("%d",&num1);
    printf("enter your 2nd number: ");
    scanf("%d",&num2);
    add(num1, num2);
    sub(num1, num2);
    div(num1, num2);
    mul(num1, num2);

    return 0;
}